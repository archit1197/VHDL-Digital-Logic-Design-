Group : Bits and Bytes
Archit Gupta 150070001 :
--------------------------------------------------------------------------------------------------------------------------------------------------------------------
    main.c is the required c File and a make file is given with it.

    we have to use make deps to create the executable and it gets created in the rel directory of the flcli file structure

    ( i.e we didnot create any new make file but used the same old one) 

----------------------------------------------------------------------------------------------------------------------------------------------------------------------

  There are 11 files top_module.vhd, comm_fpga_fx2.vhd,encrypter.vhd,decrypter.vhd,debouncer.vhd,read_multiple_data_bytes.vhd debouncer.vhd,atm_main_controller.vhd,constraints.ucf
These are the required files to create the project

we havent been able to successfully implement the ethernet part.since only bit files are generated we havent placed our ethernet files here 




